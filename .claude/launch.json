{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "webhook-server",
      "runtimeExecutable": ".venv/bin/uvicorn",
      "runtimeArgs": ["webhook_server:app", "--host", "0.0.0.0", "--port", "8000"],
      "port": 8000
    }
  ]
}
